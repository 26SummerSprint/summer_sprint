# ── 절대 커밋 금지: 개인/팀 비밀값 ─────────────────────────
arxiv_pipeline/local_config.py
.env
*.env

# ── 자동 생성되는 데이터 (각 서버/로컬에서 파이프라인 실행 시 재생성됨) ──
arxiv_pipeline/data/
arxiv_pipeline/backups/

# ── 가상환경 ──────────────────────────────────────────
venv/
.venv/

# ── 파이썬 캐시 ────────────────────────────────────────
__pycache__/
*.pyc
*.pyo

# ── 로그 ─────────────────────────────────────────────
*.log

# ── 문서(.md)는 커밋하지 않음 (기획/문서는 Notion에서 관리) ──
*.md
